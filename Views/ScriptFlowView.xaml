<UserControl x:Class="NoCodeVision.Views.ScriptFlowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource iOSTitle}" Text="{Binding SelectedFlow.Name, FallbackValue='脚本流程'}" />

        <Border Grid.Row="1" Style="{StaticResource AppleGroupCard}" Padding="14" Margin="0,12,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource AppleBtn}" Content="▶ 运行脚本" Command="{Binding RunScriptCmd}" VerticalAlignment="Center" />
                <Button Style="{StaticResource AppleBtnSecondary}" Content="⏹ 停止" Command="{Binding StopScriptCmd}" VerticalAlignment="Center" Margin="10,0,0,0" />
                <Border Style="{StaticResource AppleStatusPill}" Background="{StaticResource AccentSoftBrush}" Margin="12,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Status}" FontSize="12" Foreground="{StaticResource AccentBrush}" />
                </Border>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource AppleGroupCard}" Padding="0">
            <TextBox Style="{StaticResource AppleCodeEditor}"
                     Text="{Binding SelectedFlow.ScriptContent, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Stretch" HorizontalAlignment="Stretch" />
        </Border>

        <Border Grid.Row="3" Style="{StaticResource AppleGroupCard}" Padding="14" Margin="0,12,0,0">
            <TextBlock Style="{StaticResource AppleSubLabel}" Text="Lua 脚本将在运行时按顺序执行 vision.match / plc.write 等接口调用。" TextWrapping="Wrap" />
        </Border>
    </Grid>
</UserControl>
