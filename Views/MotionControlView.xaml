<UserControl x:Class="NoCodeVision.Views.MotionControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 左侧子导航 -->
        <Border Grid.Column="0" Style="{StaticResource AppleGroupCard}" Padding="12" Margin="0,0,12,0" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Style="{StaticResource AppleSectionHeader}" Text="运控" Margin="2,0,0,8" />
                <ListBox ItemsSource="{Binding Tabs}" SelectedItem="{Binding SelectedTab}"
                         Style="{StaticResource ApplePillList}" />
            </StackPanel>
        </Border>

        <!-- 右侧内容 -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- 轴 -->
                <Border x:Name="AxisPage" Padding="14" Margin="0,0,0,12">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource AppleGroupCard}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedTab}" Value="轴">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Style="{StaticResource AppleSectionHeader}" Text="轴" />
                        <ListView ItemsSource="{Binding Axes}" Margin="0,6,0,0" BorderThickness="0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="90" />
                                    <GridViewColumn Header="状态" DisplayMemberBinding="{Binding Status}" Width="70" />
                                    <GridViewColumn Header="当前位置" DisplayMemberBinding="{Binding Value, StringFormat={}{0:F2}}" Width="90" />
                                    <GridViewColumn Header="单位" DisplayMemberBinding="{Binding Unit}" Width="50" />
                                    <GridViewColumn Header="使能" DisplayMemberBinding="{Binding Enabled}" Width="60" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- IO -->
                <Border x:Name="IoPage" Padding="14" Margin="0,0,0,12">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource AppleGroupCard}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedTab}" Value="IO">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Style="{StaticResource AppleSectionHeader}" Text="IO" />
                        <ListView ItemsSource="{Binding IoPoints}" Margin="0,6,0,0" BorderThickness="0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="100" />
                                    <GridViewColumn Header="地址" DisplayMemberBinding="{Binding Address}" Width="70" />
                                    <GridViewColumn Header="类型" DisplayMemberBinding="{Binding Type}" Width="70" />
                                    <GridViewColumn Header="状态" DisplayMemberBinding="{Binding Status}" Width="70" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- 气缸 -->
                <Border x:Name="CylinderPage" Padding="14" Margin="0,0,0,12">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource AppleGroupCard}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedTab}" Value="气缸">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Style="{StaticResource AppleSectionHeader}" Text="气缸" />
                        <ListView ItemsSource="{Binding Cylinders}" Margin="0,6,0,0" BorderThickness="0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="120" />
                                    <GridViewColumn Header="状态" DisplayMemberBinding="{Binding Status}" Width="80" />
                                    <GridViewColumn Header="动作" DisplayMemberBinding="{Binding Action}" Width="80" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- 轴点位表 -->
                <Border x:Name="PointPage" Padding="14" Margin="0,0,0,12">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource AppleGroupCard}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedTab}" Value="轴点位表">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Style="{StaticResource AppleSectionHeader}" Text="轴点位表" />
                        <ListView ItemsSource="{Binding PointTable}" Margin="0,6,0,0" BorderThickness="0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="点位名" DisplayMemberBinding="{Binding Name}" Width="100" />
                                    <GridViewColumn Header="X" DisplayMemberBinding="{Binding X, StringFormat={}{0:F1}}" Width="70" />
                                    <GridViewColumn Header="Y" DisplayMemberBinding="{Binding Y, StringFormat={}{0:F1}}" Width="70" />
                                    <GridViewColumn Header="Z" DisplayMemberBinding="{Binding Z, StringFormat={}{0:F1}}" Width="70" />
                                    <GridViewColumn Header="说明" DisplayMemberBinding="{Binding Desc}" Width="140" />
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- 料盘 -->
                <Border x:Name="TrayPage" Padding="14">
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource AppleGroupCard}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedTab}" Value="料盘">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <TextBlock Style="{StaticResource AppleSectionHeader}" Text="料盘" />
                        <ItemsControl ItemsSource="{Binding TrayCells}" Margin="0,8,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="{Binding TrayRows}" Columns="{Binding TrayCols}" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="3" Height="46" CornerRadius="8" BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#F2F2F7" />
                                                <Setter Property="BorderBrush" Value="#C7C7CC" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Occupied}" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource WarningSoftBrush}" />
                                                        <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Label}" FontSize="12" FontWeight="SemiBold"
                                                       Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" />
                                            <TextBlock Text="{Binding Product}" FontSize="10"
                                                       Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Center" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
