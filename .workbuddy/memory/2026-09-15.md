# 2026-09-15 工作日志

## 新建项目示例扩展（更多 / 说明 / 内容丰富）
- ViewModels/ViewModels.cs 的 ProjectExample 类重写为 **9 个场景示例**：
  1. 视觉定位检测（相机+MQTT）
  2. 离线图片批量检测（读文件+网口）
  3. 硬件触发产线飞拍（相机+PLC串口）
  4. 几何尺寸测量（相机+UDP）
  5. 字符识别 OCR（相机+MQTT）
  6. 颜色缺陷分拣（相机+网口）
  7. 多相机拼接检测（双相机+MQTT）
  8. 目标计数统计（读文件夹+UDP）
  9. 模板与精度标定（相机+本地，不外发）
- 新增字段 `LongDescription`（多行详细说明）、`Features`（特性标签 List<string>）。
- `BuildCommConfigs` 按示例返回 1~3 路通讯（MQTT/网口/UDP/串口 组合）。
- `BuildFlows` 每个示例返回 2 个流程（主检测 + 校准/统计/管理），步骤覆盖采集/预处理/模板匹配/
  几何测量/缺陷/OCR/颜色/计数/拼接/外发；多流程随 .ncv 持久化。
- `ToProjectItem` 改用 `LongDescription` 写入 Description，Tags 追加 Features。
- Views/ProjectView.xaml 弹窗：ItemsControl 改 WrapPanel 两列；卡片展示 Name + LongDescription 说明 +
  Features 特性标签（黄色小标签）+ 相机/触发/采集源/外发 元数据标签。
- 构建 -c Debug：**0 错误**（14 警告均为 Comm/Lua 既有可空代码，无关）。
- 工具链约束（复用）：脚本存 `.txt` 经 PowerShell 调 python 执行，规避 Write 加密 .py 与 Bash 中文损坏；
  删文件走 `python os.remove`（沙箱 rm 无效）。

## 构建命令（沙箱可用）
`PowerShell: & 'C:\Program Files\dotnet\dotnet.exe' build 'D:\wqz\code\NoCodeVision\NoCodeVision.csproj' -c Debug`
