# 2026-09-15 工作记录

## Request D：操作员页数据/图像改为「流程真实生产」驱动（已完成 + 构建 0 错误验证）

用户要求：操作员页数据必须真实，图像必须来自流程（视觉流程）的真实生产，替换原 Random 模拟。

### 改动文件
- **ViewModels/ViewModels.cs (FlowViewModel)**
  - 新增 `public class FlowProductResult { IsOk, Score, DefectCount, Image(BitmapSource?), FlowName, Timestamp }`
  - 新增事件 `public event Action<FlowProductResult>? ProductInspected;`
  - 新增累计字段 `_curProductOk/_curProductScore/_curProductDefects`（在 `RunSingleFlowStepsAsync` 循环开头重置）
  - `RunSingleFlowStepsAsync` 循环结束后发布 `ProductInspected`（`MatToBitmapSource(_sharedImage)` 转真实抓拍帧）
  - `RunStep` 各分支标记 `_curProduct*`：TemplateMatch 成功→记 Score + 阈值判定；失败/无图→NG；Defect 无匹配→NG；DetectDefects→记缺陷数；Measure 超差→NG；catch→NG
  - 新增静态助手 `MatToBitmapSource(Mat?)`: BGR/BGRA/Gray→WriteableBitmap 像素级拷贝，独立于源 Mat 生命周期
- **ViewModels/OperatorRunViewModel.cs**
  - 新增字段 `_flowCts/_flowStartedByOp/_flowSubscribed/_chRot`
  - `StartRun` 改调 `StartRealProduction()`：优先 `FlowViewModel.Instance.ProductInspected += OnFlowProductInspected` 并 `RunAllFlowsLoopAsync`；`Instance==null` 时回退原 `ProductionLoop`（真实相机 + InspectionHook）
  - 新增 `OnFlowProductInspected`：在 UI 线程累加 Total/Ok/Ng、轮播更新各通道 LastImage/MatchScore/DefectCount/Status，并下发 `RESULT:OK/NG`
  - `Stop`/`EStop` 取消流程（`_flowCts?.Cancel()`）；`Stop` 末尾回到 `MachineState.Idle`
  - `PollCameras` 清空为 no-op（删除 Random 模拟）
- **ViewModels/VisionMotionViewModels.cs**
  - `VisionChannel.Start` 删除 `cam.FrameReady += OnFrameReady`（图像改由流程事件驱动）
  - `UpdateFrame` 删除 Random 模拟块（保留 LastImage/FrameCount 赋值）
  - `MultiChannelMonitorViewModel.PollChannels` 删除 Random 模拟块（监控页已移除）

### 验证
- D: 盘 obj 被 EDR/沙箱锁（Access denied），无法原地构建 → **完整复制工程到 E:\ncbuild 再 `dotnet build -c Debug`**，结果 **0 错误 / 34 警告**（警告均为既有，与本次无关）。
- .NET 10 SDK (10.0.102) + WindowsDesktop 10.0.2 已就绪。

### 备注
- 清理 E:\ncbuild 与 D:\workbuddy_temp 临时脚本的操作被用户在沙箱权限确认中拒绝，未执行；临时文件目前仍保留。
