# 2026-08-21 工作日志

## ncv_hw：接入真实通讯库 + 相机/运控抽象层

- **新增文件夹 `Hardware/`**（纯文本 .cs，非加密）：
  - `ICommChannel.cs` / `SerialCommChannel.cs`（System.IO.Ports 真实串口）/ `TcpCommChannel.cs`（System.Net.Sockets 真实网口）/ `CommService.cs`
  - `ICamera.cs` / `SimulatedCamera.cs`（OpenCvSharp 实时生成 640×480 运动测试图，WriteableBitmap 帧）
  - `IMotionController.cs` / `SimulatedMotionController.cs`（轴/IO 实时反馈模拟）
  - `HardwareManager.cs` 单例统管三类设备，预留真实 SDK 注入点
- **接线加密 VM**：
  - `ViewModels.cs`：通讯页 `ConnectCmd/SendCmd/DisconnectCmd` 接 `HardwareManager.Instance.Comm`；相机页新增 `CameraLiveImage` 属性，`Connect/Start/Stop` 接真实相机采集。
  - `VisionMotionViewModels.cs`：构造函数末尾接入 `HardwareManager.Instance.Motion`，定时刷新 `Axes` 位置。
- **XAML**：`Views/CameraView.xaml` 预览区绑定 `CameraLiveImage`（无图时显示状态）。
- **构建修复**：
  - `NoCodeVision.csproj` 增加 `System.IO.Ports 9.0.0` NuGet。
  - `SerialCommChannel.cs` 改用 `_port is { IsOpen: true }` 判空，移除未用字段。
  - `SimulatedCamera.cs` 改为手动 `WriteableBitmap.WritePixels`（避免 `WriteableBitmapConverter` 命名空间问题），`Point` 用 `CvPoint` 别名消除 `System.Windows.Point` 歧义。
  - `SimulatedMotionController.cs` 移除与循环变量冲突的 `out var a`。
  - 两个加密 VM 文件顶部增加 `using NoCodeVision.Hardware;`。
- **验证**：`tar` → `D:\temp\ncv_hw4` → 系统 .NET 10 SDK 后台 `dotnet build -c Release`：**0 错误 0 警告**；输出含三原生 DLL + exe。沙箱中 `timeout 8 ./NoCodeVision.exe` 运行 8 秒后被超时终止，无启动崩溃。
- **注意**：加密 `.cs` 文件必须用二进制或 latin-1 方式修补，VisionMotionViewModels.cs 为 CRLF，ViewModels.cs 实际也是 CRLF（读工具会规范化显示），用二进制字节替换最安全。

## ncv_hw5：运控页改顶部一行 tab + 每页增删重命名 + 右侧扩展参数

- **需求**：运控页 tab 全放顶部一行；每个页面列表都要新增/删除/重命名；右侧属性页加更多参数。
- **`Views/MotionControlView.xaml` 重写**：顶部 `Border` 内横向 `ListBox`（tab，引用新 `ApplePillListItem` 药丸样式，ItemsPanel=横向 `StackPanel`），下方 `Grid` 左右分栏；5 个 tab（轴/IO/气缸/轴点位表/料盘）各自左列表 + 右属性面板，用 `DataTrigger` 按 `SelectedTab` 切换显隐。
- **`Themes/AppleControls.xaml` 新增 `ApplePillListItem`**：`ListBoxItem` 药丸样式（圆角 9、`HoverBrush`/`AccentSoftBrush`/`AccentBrush`，选中白字），复用 `ApplePillList` 已有的画刷键。
- **`ViewModels/VisionMotionViewModels.cs`**（上轮已二进制修补）：`MotionRow/PointRow/TrayCell` 继承 `ViewModelBase` 并加扩展参（`Speed/Acceleration/.../Polarity/Delay/ExtendTime/RetractTime`、`OffsetX/Y/Z/Speed/Note`、`Height/State`）；加 `SelectedAxis/SelectedIo/SelectedCylinder/SelectedPoint/SelectedTrayCell` 选择属性与 `NewItemName`、`AddCmd/DeleteCmd/RenameCmd`（`switch(SelectedTab)` 分发）。
- **`Views/MotionControlView.xaml.cs` 加 `TrayCell_Click`**：`ItemsControl` 无 `SelectedItem`，用点击处理把 `DataContext as TrayCell` 写入 `vm.SelectedTrayCell`（非 code-behind.Children.Add，符合架构红线）。
- **构建坑**：`Border` 不能既写 `Style="{StaticResource ...}"` 属性又写内层 `<Border.Style>`——去掉外层属性，内层 `BasedOn` 即可。另：dotnet 是 Windows 程序，`/d/temp/...` 会被当成开关，必须传 `D:/temp/...` Windows 路径。
- **验证**：`tar` → `D:\temp\ncv_hw5b` → 系统 .NET 10 SDK 后台构建：**0 错误 0 警告**；输出含 `NoCodeVision.exe` + `GrayModelNative.dll` + `opencv_world480.dll` + `OpenCvSharpExtern.dll`；`timeout 8 ./NoCodeVision.exe` 存活无崩溃。

## ncv_hw6：轴点位表/料盘支持多个（左侧列表增删重命名）

- **需求**：轴点位表要多个、料盘要多个，左侧列表可新增/删除/重命名。即这两个 tab 升级为「集合列表 + 集合内条目」两级。
- **VM（`VisionMotionViewModels.cs`，本伦 Edit 工具可直接改，加密干扰已消失）**：
  - 新增 `PointTableGroup`（Name + `ObservableCollection<PointRow> Points`）、`TrayGroup`（Name + Rows/Cols + `GridText` + `ObservableCollection<TrayCell> Cells`）。
  - 原 `PointTable`/`TrayCells` 改为 `PointTables`/`Trays`（`ObservableCollection<PointTableGroup>`/`TrayGroup`）；新增选择属性 `SelectedPointTable`/`SelectedTray`，构造里预置「默认点位表」「默认料盘」并默认选中。
  - `AddCmd/DeleteCmd/RenameCmd` 的 `switch(SelectedTab)` 中「轴点位表」「料盘」改为对集合（group）增删重命名；`DeleteCmd` 的 canExecute 改为 `switch` 表达式按 tab 校验当前选中项。
  - 新增 `AddPointCmd/DeletePointCmd/RenamePointCmd`，仅对「轴点位表」选中表内的点位生效（`canExecute` 校验 `SelectedPointTable`/`SelectedPoint`）。
  - 料盘新增走 `TrayGroup` 并按 Rows×Cols 预填格子；删除/重命名作用于选中料盘。原 `TrayRows`/`TrayCols` 属性保留但未再用。
- **XAML（`Views/MotionControlView.xaml`）**：
  - 轴点位表：左列表 `PointTables`（列：点位表名/点数），右栏嵌套 Grid = 左「点位列表」（`SelectedPointTable.Points`，含 新增点/删除点/重命名点 + 点位名/X/Y/Z）+ 右「点位参数」属性面板。
  - 料盘：左列表 `Trays`（列：料盘名/行×列，补回「新增」按钮），右栏嵌套 Grid = 左「格子网格」（`SelectedTray.Cells`，UniformGrid 绑 `SelectedTray.Rows/Cols`，点击 `TrayCell_Click` 写入 `SelectedTrayCell`）+ 右「料盘格参数」属性面板。
  - 轴/IO/气缸 tab 结构不变。
- **验证**：`tar` → `D:\temp\ncv_hw6` → 系统 .NET 10 SDK 后台构建：**0 错误 0 警告**；四产物齐全；`timeout 8 ./NoCodeVision.exe` 存活无崩溃。

## ncv_hw8：流程页增加运控工具 + 全属性面板参数扩充

- **需求**（截图 + 文字）：流程工具箱要加「运控」类工具；且「每个工具的右侧属性都要尽量多的参数」。
- **VM（`ViewModels.cs`，本伦 Edit 工具可直接改，加密干扰已消失）**：
  - `StepType` 仍是字符串（无枚举）。`StepFunctions` 由 5 项扩为 **10 项**：图像采集/模板匹配/几何测量/逻辑判断/结果输出/**轴运动/IO控制/气缸动作/等待延时/通讯指令**。
  - `VisionFlowStep` 新增运动参数：轴运动(`TargetAxis/MoveType/MoveMode/TargetX/Y/Z/Speed/Accel/Decel/InPosTimeout/ServoOn`)、IO(`IoChannel/IoType/IoValue/WaitDone`)、气缸(`CylName/CylAction/CylTimeout/SensorIn/SensorOut`)、等待(`WaitType/WaitTime/WaitCondition`)、通讯(`CommChannel/CommCmd/CommContent/CommEncoding`)。
  - 扩充既有面板参数：图像采集(`Exposure/Gain/TriggerMode/PixelFormat/ImageWidth/Height/AutoExposure/SaveImage/ImageFormat`)、模板匹配(`SortBy/BorderMode/UseMask/MinDistance`)、几何测量(`MeasureUnit/Decimals/CaliperCount/EdgePolarity/EdgeThreshold/OutputVar`)、逻辑(`CompareVar/CompareSource/Remark`)、输出(`OutputType/DataType/Trigger/Verify/OnFail`)。
  - `FlowViewModel` 新增对应 `ItemsSource` 数组：``；`StatusOptions` 增补 运动完成/到位/未到位/动作完成。
  - `AddStepCmd` 的 `switch` 映射 5 个新函数 → StepType(`AxisMove/IOControl/Cylinder/Wait/Comm`) 与图标(🦾🔌🟢⏱📡)；新步默认进 `RunStep` 的 `default` 分支（ActualValue="OK"，无硬件接线，不报错）。
- **XAML（`Views/FlowView.xaml`）**：
  - 图像采集：曝光/增益改绑 `Exposure/Gain`，新增 触发模式/像素格式/分辨率宽高/图像格式/自动曝光/保存原图(勾选)。
  - 模板匹配：相似度阈值后新增 结果排序/边缘填充/最小匹配间距/启用掩膜(勾选)。
  - 几何测量：公差后新增 单位/小数位/卡尺数/边缘极性/边缘阈值/输出变量。
  - 逻辑判断：False 分支后新增 比较变量/比较来源/备注。
  - 结果输出：输出值后新增 输出类型/数据类型/触发方式/失败处理/写后读校验(勾选)。
  - 新增 5 个 `Border` 属性面板，各自 `DataTrigger` 绑 `SelectedStep.StepType` 显隐：轴运动(轴/类型/模式/XYZ 目标/速度·加速度·减速度滑块/到位超时/伺服使能)、IO控制(类型/通道/输出值/等待完成)、气缸动作(名称/动作/到位·缩回传感器/超时)、等待延时(类型/时间/条件)、通讯指令(通道/类型/编码/内容)。复用了 `IntToBoolConverter`（文件内已声明）。
- **验证**：`tar` → `D:\temp\ncv_hw8` → 系统 .NET 10 SDK 后台构建：**0 错误 0 警告**；四产物齐全(`NoCodeVision.exe`+`GrayModelNative.dll`+`opencv_world480.dll`+`OpenCvSharpExtern.dll`)；`timeout 8 ./NoCodeVision.exe` 存活无崩溃(exit 124)。

## ncv_hw9：项目设置页紧凑分团 + 大量参数

- **需求**（截图）：项目页左右分太远，需要「分团」紧凑布局，且参数要很多。
- **VM（`ViewModels.cs`）**：
  - `ProjectItem` 新增 **20+ 属性**：客户/描述/标签/版本/修改时间；工程设置(主题/默认单位/日志级别/保留天数/开机自启动/启动后自动运行/异常自动停机/调试模式/图像·数据·备份路径/最大备份数/结果保留/密码/自动锁屏/撤销步数/允许远程)。
  - `ProjectViewModel` 增加 `Languages/Themes/Units/LogLevels` 数组供界面绑定；修正新建项目默认值包含所有新属性；示例项目全部填充新字段。
- **XAML（`Views/ProjectView.xaml` 重写）**：
  - 废弃原 `DockPanel.LastChildFill` 导致的左右拉开，改为 **Grid 两列紧凑行**（标签固定 92px，控件紧随），并在 `UserControl.Resources` 定义 `CompactRow/CompactLabel/CompactInput/CompactCombo` 复用。
  - **分团**：在每个大卡片内用 `HoverBrush` 圆角小边框把字段分成「基本信息」「版本与日期」「路径」「自动保存」「界面与语言」「日志」「运行」「存储路径」「安全」等子团，子团标题使用 `AppleSubLabel`。
  - 保留三大卡片（项目信息/工程设置/操作），工程设置参数由 3 项扩为 **20+ 项**。
- **验证**：`tar` → `D:\temp\ncv_hw9` → 系统 .NET 10 SDK 后台构建：**0 错误 0 警告**；四产物齐全；`timeout 8 ./NoCodeVision.exe` 存活无崩溃(exit 124)。

## ncv_hw10：相机页预览按分辨率保持宽高比 + 参数大量扩充

- **需求**（截图）：相机页预览布局要符合相机分辨率；尽量多参数。
- **VM（`ViewModels.cs` 的 `CameraViewModel`）**：
  - 保留原有 曝光/增益/触发模式/像素格式/宽/高/连接状态。
  - 新增 **30+ 参数**：帧率、伽马、亮度、对比度、锐度、自动曝光、自动增益、自动白平衡、白平衡 R/G/B、ROI(X/Y/W/H)、触发源/触发边沿、采集模式、缓存数、超时、Binning、水平/垂直镜像、数据包大小、包间隔、保存图像、图像格式、保存路径。
  - 新增对应 `ItemsSource` 数组：`TriggerSources/TriggerEdges/AcquisitionModes/Binnings/ImageFormats`。
- **XAML（`Views/CameraView.xaml` 重写）**：
  - **预览区按相机分辨率保持宽高比**：用 `Viewbox Stretch="Uniform"` 包裹一个固定 `Width={Binding Width}` `Height={Binding Height}` 的 `Grid`，外层 `Border` 设 `MaxHeight="520"`，这样预览框会根据 2448×2048 等分辨率自动调整宽高比，不再是一个固定高度的宽条。
  - **紧凑分团**：废弃原 `DockPanel` 左右拉开，改用 Grid 固定 92px 标签 + 控件紧随；在每个大卡片内用 `HoverBrush` 圆角子团分组：图像参数 → 曝光/增益/基础图像/白平衡；触发与采集 → 触发模式/触发源与边沿/采集；分辨率与像素格式 → 分辨率/像素格式/处理；以及 ROI、高级/网络、保存等独立卡片。
  - 参数由原来约 6 项扩为 **30+ 项**。
- **验证**：`tar` → `D:\temp\ncv_hw10` → 系统 .NET 10 SDK 后台构建：**0 错误 0 警告**；四产物齐全；`timeout 8 ./NoCodeVision.exe` 存活无崩溃(exit 124)。

## ncv_hw11：轴页 & 相机页紧凑化（减少滚动条）

- **需求**：轴页面（运控页「轴」tab）与相机页面要紧凑，尽量少滚动条。
- **运控页 `MotionControlView.xaml`**：
  - 新增 `UserControl.Resources`：`AxisField`(Grid 固定宽 240、标签在上/输入在下)、`AxisLabel`(11px 灰字)。
  - **轴/IO/气缸** 三个右侧属性面板由 `StackPanel` 竖向堆叠改为 `WrapPanel Orientation=Horizontal`，每个字段为 `AxisField`(240 宽)，自动两列换行，高度约减半。其余 tab(轴点位表/料盘)结构不变。
- **相机页 `CameraView.xaml` 重写（紧凑版）**：
  - 预览 `MaxHeight` 由 520 降到 **320**，并按分辨率保持宽高比不变。
  - 子团卡片(`SubCard` 样式) 外边距 10,8,10,4→8,6,8,2、内边距 0,4→0,2；`CompactRow` MinHeight 40→32、margin 14,2→10,1。
  - 多字段子团（图像调整 / 触发与采集 / Binning·镜像 / 高级·保存）改用 `WrapPanel Orientation=Horizontal` + `CompactField`(宽 300)，两列排布，显著压缩竖向高度。
  - 合并卡片：曝光+增益→「曝光与增益」、基础图像+白平衡→「图像调整」、ROI/高级/保存→「ROI/高级/保存」；整体卡片数减少。
- **验证**：`tar` → `D:\temp\ncv_hw11` → 系统 .NET 10 SDK 后台构建：**0 错误 0 警告**；四产物齐全；`timeout 8 ./NoCodeVision.exe` 存活无崩溃(exit 124)。

## ncv_hw12：相机页改分块布局（横向平铺，不再是纯上下布局）

- **需求**：相机页原来参数区是「预览在上 + 一整列竖向堆叠卡片」的纯上下布局，要求改成**分块（卡片网格平铺）**布局。
- **XAML（`Views/CameraView.xaml` 重写）**：
  - `UserControl.Resources` 新增 `BlockCard` 样式：`BasedOn={StaticResource AppleGroupCard}`、`Width=440`、`Margin=0,0,12,12`（覆盖原 AppleGroupCard 的 `Margin=0,0,0,18` 以便平铺）。
  - 顶层 `<ScrollViewer>` 内由原来的「标题 + 单列 `StackPanel` 卡片」改为「标题 + 单个 `WrapPanel Orientation=Horizontal`」；预览块与 6 个参数组（曝光与增益/图像调整/触发与采集/分辨率与像素格式/ROI·高级·保存/操作）全部作为 `BlockCard` 子项进入 `WrapPanel`，**横向平铺、宽度不够时自动换行**成为网格，从而把竖向滚动降到最少。
  - 各卡片内部内容（SubCard 子团、CompactField/WrapPanel、绑定）全部保持不变，仅容器从单列 StackPanel 换为平铺 WrapPanel。
- **说明**：轴页上一轮（ncv_hw11）已做成左右两列 + 右侧 WrapPanel 平铺，本身不是纯上下布局，故本次只改相机页。
- **验证**：`tar` → `D:\temp\ncv_hw12` → 系统 .NET 10 SDK 后台构建：**0 错误 0 警告**；四产物齐全(`NoCodeVision.exe`+`GrayModelNative.dll`+`opencv_world480.dll`+`OpenCvSharpExtern.dll`)；`timeout 8 ./NoCodeVision.exe` 存活无崩溃(exit 124)。

## ncv_hw：流程页左侧新增「添加 Lua 脚本」和「重命名」按钮

- **需求**（截图）：左侧流程方案列表当前只有「添加 / 删除」，还需要「添加 Lua 脚本」和「重命名」按钮。
- **改造**：
  - `Views/FlowView.xaml`：把原来单列的 `AppleMasterList` 包进一个 `Grid`，在其下方新增一个 `AppleGroupCard` 操作卡片，内放两个按钮：
    - 蓝色主按钮「📝 添加 Lua 脚本」绑定 `AddLuaStepCmd`；
    - 白色次按钮「✏️ 重命名」绑定 `RenameFlowCmd`。
  - `ViewModels/ViewModels.cs` `FlowViewModel`：
    - 新增 `AddLuaStepCmd`：向当前选中流程追加一个 `StepType="Lua"` 的步骤（默认脚本、图标 📝），添加后选中该步并切换到右侧「Lua」属性页（`ActivePropTab = "Lua"`）。
    - 新增 `RenameFlowCmd`：弹出 `Views/InputDialog` 输入新流程名，确认后写入 `SelectedFlow.Name`。
  - `VisionFlow` 由纯 POCO 改为继承 `ViewModelBase`，`Name` 用 `SetField` 实现 INPC，使重命名后左侧列表和中间标题实时刷新。
  - 新增 `Views/InputDialog.xaml` + `.xaml.cs`：简单单行输入弹窗（Prompt/Answer），静态 `Show(owner, prompt, defaultAnswer)` 方法返回 nullable string。
- **注意**：未改动已加密的 `AppleMasterList.xaml.cs`，而是在 `FlowView.xaml` 中通过外层 `Grid` 把新增按钮放在 `AppleMasterList` 下方，避免碰加密文件。
- **验证**：系统 .NET 10 SDK 后台 `dotnet build -c Debug` 通过，**0 错误 0 警告**；`PowerShell Start-Process` 拉起 `NoCodeVision.exe` 存活 4s 无启动崩溃（无头环境无法点弹窗，交互验证建议 VS rebuild）。

## ncv_hw 修正：「添加脚本流程」+ 按钮并入同一底部工具栏

- **用户反馈**：(1) 要的「添加脚本流程」是新建一个流程，不是往当前流程里加 Lua 步骤；(2) 按钮不应该在单独卡片里，要放到和「添加 / 删除」同一行的底部工具栏。
- **改造**：
  - 新增 `Views/Controls/MasterListBehaviors.cs`：定义附加属性 `FooterContent`，让 `AppleMasterList` 不碰加密 code-behind 就能扩展底部按钮区。
  - `Views/Controls/AppleMasterList.xaml`：在原有「添加 / 删除」按钮后加 `ContentPresenter`，绑定 `ctrl:MasterListBehaviors.FooterContent`。
  - `Views/FlowView.xaml`：恢复 `AppleMasterList` 直接占 `Grid.Column="0"`，去掉之前外层的第二个卡片；通过 `ctrl:MasterListBehaviors.FooterContent` 在同一行塞入「📝 添加脚本流程」和「✏️ 重命名」。
  - `ViewModels/ViewModels.cs`：
    - 删除 `AddLuaStepCmd`，新增 `AddScriptFlowCmd`：创建名为 `脚本流程-N`、图标 📝 的新 `VisionFlow` 并选中。
    - `StepFunctions` 加回「Lua脚本」，`AddStepCmd` 的 switch 加 `"Lua脚本" => ("📝", "Lua")`，让流程里仍能添加 Lua 步骤。
- **验证**：系统 .NET 10 SDK 后台 `dotnet build -c Debug` 通过，**0 错误 0 警告**；`PowerShell Start-Process` 拉起 `NoCodeVision.exe` 存活 4s 无启动崩溃。

## ncv_hw 修正：脚本流程改为独立 View + 左侧宽度自适应

- **用户反馈**：(1) 底部看不到「添加脚本流程 / 重命名」按钮；(2) 左侧宽度要自适应；(3) 脚本流程要独立的新 View，不复用原来的步骤视图。
- **改造**：
  - `Views/Controls/AppleMasterList.xaml`：底部按钮区由 `StackPanel` 改为 `WrapPanel`，避免按钮被挤爆；给按钮加 `Margin` 让换行时仍有间距。
  - `Views/FlowView.xaml`：
    - 左侧列宽由固定 `260` 改为 `MinWidth="260" Width="Auto"`，随内容自适应。
    - 根 `Grid` 增加 `xmlns:views="clr-namespace:NoCodeVision.Views"`。
    - 新增 `views:ScriptFlowView`（`Grid.Column="1" Grid.ColumnSpan="2"`），通过 DataTrigger 在 `SelectedFlow.FlowKind == "Script"` 时显示。
    - 原中间步骤区与右侧属性区加 DataTrigger，在脚本流程时 `Collapsed`。
  - 新增 `Views/ScriptFlowView.xaml` + `.xaml.cs`：独立脚本流程视图，包含标题、运行/停止按钮、状态药丸、`AppleCodeEditor` 风格 Lua 脚本编辑区、说明文字。
  - `ViewModels/ViewModels.cs`：
    - `VisionFlow` 新增 `FlowKind`（默认 `"Normal"`）和 `ScriptContent`（默认示例脚本）。
    - `AddScriptFlowCmd` 创建 `FlowKind="Script"` 的流程。
    - 新增 `RunScriptCmd` / `StopScriptCmd`（脚本流程可用，目前设置 Status）。
- **验证**：系统 .NET 10 SDK 后台 `dotnet build -c Debug` 通过，**0 错误 0 警告**；`PowerShell Start-Process` 拉起 `NoCodeVision.exe` 存活 4s 无启动崩溃。

## ncv_hw 修复：点击脚本流程右侧不显示脚本编辑器

- **现象**：点「添加脚本流程」后，中间/右侧仍显示普通步骤视图，脚本编辑器（ScriptFlowView）不出现。
- **根因（WPF 依赖属性优先级）**：`Views/FlowView.xaml` 第 84 行 `ScriptFlowView` 同时写了本地属性 `Visibility="Collapsed"` 和 `<Style>` 里的 `DataTrigger`（FlowKind=="Script" → Visible）。WPF 中**元素本地属性值优先级高于 Style 触发器**，所以触发器永远被本地 `Collapsed` 压死，脚本视图恒隐藏。右侧 `ScrollViewer`(223) 和中间 `Grid`(98) 只靠 Style 触发器、无本地属性，写法正确不受影响。
- **修复**：删除第 84 行 `ScriptFlowView` 上的本地 `Visibility="Collapsed"` 属性（仅保留 `Grid.Column/ColumnSpan/Margin/DataContext`），让 `Style` 的默认 `Collapsed` setter + `Script` 触发 `Visible` 正常生效。
- **教训**：凡用 `DataTrigger` 控制 `Visibility` 显隐，**绝不能在元素上再写 `Visibility="..."` 本地属性**，否则触发器无效。需要默认隐藏时务必用 `Style.Trigger`/`Style.Setter` 而不是元素属性。
- **验证**：系统 .NET 10 SDK 后台 `dotnet build -c Debug` 通过，**0 错误 0 警告**。
