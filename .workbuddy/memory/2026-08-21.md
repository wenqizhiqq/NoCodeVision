# 2026-08-21 工作日志

## ncv_hw：接入真实通讯库 + 相机/运控抽象层

- **新增文件夹 `Hardware/`**（纯文本 .cs，非加密）：
  - `ICommChannel.cs` / `SerialCommChannel.cs`（System.IO.Ports 真实串口）/ `TcpCommChannel.cs`（System.Net.Sockets 真实网口）/ `CommService.cs`
  - `ICamera.cs` / `SimulatedCamera.cs`（OpenCvSharp 实时生成 640×480 运动测试图，WriteableBitmap 帧）
  - `IMotionController.cs` / `SimulatedMotionController.cs`（轴/IO 实时反馈模拟）
  - `HardwareManager.cs` 单例统管三类设备，预留真实 SDK 注入点
- **接线加密 VM**：
  - `ViewModels.cs`：通讯页 `ConnectCmd/SendCmd/DisconnectCmd` 接 `HardwareManager.Instance.Comm`；相机页新增 `CameraLiveImage` 属性，`Connect/Start/Stop` 接真实相机采集。
  - `VisionMotionViewModels.cs`：构造函数末尾接入 `HardwareManager.Instance.Motion`，定时刷新 `Axes` 位置。
- **XAML**：`Views/CameraView.xaml` 预览区绑定 `CameraLiveImage`（无图时显示状态）。
- **构建修复**：
  - `NoCodeVision.csproj` 增加 `System.IO.Ports 9.0.0` NuGet。
  - `SerialCommChannel.cs` 改用 `_port is { IsOpen: true }` 判空，移除未用字段。
  - `SimulatedCamera.cs` 改为手动 `WriteableBitmap.WritePixels`（避免 `WriteableBitmapConverter` 命名空间问题），`Point` 用 `CvPoint` 别名消除 `System.Windows.Point` 歧义。
  - `SimulatedMotionController.cs` 移除与循环变量冲突的 `out var a`。
  - 两个加密 VM 文件顶部增加 `using NoCodeVision.Hardware;`。
- **验证**：`tar` → `D:\temp\ncv_hw4` → 系统 .NET 10 SDK 后台 `dotnet build -c Release`：**0 错误 0 警告**；输出含三原生 DLL + exe。沙箱中 `timeout 8 ./NoCodeVision.exe` 运行 8 秒后被超时终止，无启动崩溃。
- **注意**：加密 `.cs` 文件必须用二进制或 latin-1 方式修补，VisionMotionViewModels.cs 为 CRLF，ViewModels.cs 实际也是 CRLF（读工具会规范化显示），用二进制字节替换最安全。

## ncv_hw5：运控页改顶部一行 tab + 每页增删重命名 + 右侧扩展参数

- **需求**：运控页 tab 全放顶部一行；每个页面列表都要新增/删除/重命名；右侧属性页加更多参数。
- **`Views/MotionControlView.xaml` 重写**：顶部 `Border` 内横向 `ListBox`（tab，引用新 `ApplePillListItem` 药丸样式，ItemsPanel=横向 `StackPanel`），下方 `Grid` 左右分栏；5 个 tab（轴/IO/气缸/轴点位表/料盘）各自左列表 + 右属性面板，用 `DataTrigger` 按 `SelectedTab` 切换显隐。
- **`Themes/AppleControls.xaml` 新增 `ApplePillListItem`**：`ListBoxItem` 药丸样式（圆角 9、`HoverBrush`/`AccentSoftBrush`/`AccentBrush`，选中白字），复用 `ApplePillList` 已有的画刷键。
- **`ViewModels/VisionMotionViewModels.cs`**（上轮已二进制修补）：`MotionRow/PointRow/TrayCell` 继承 `ViewModelBase` 并加扩展参（`Speed/Acceleration/.../Polarity/Delay/ExtendTime/RetractTime`、`OffsetX/Y/Z/Speed/Note`、`Height/State`）；加 `SelectedAxis/SelectedIo/SelectedCylinder/SelectedPoint/SelectedTrayCell` 选择属性与 `NewItemName`、`AddCmd/DeleteCmd/RenameCmd`（`switch(SelectedTab)` 分发）。
- **`Views/MotionControlView.xaml.cs` 加 `TrayCell_Click`**：`ItemsControl` 无 `SelectedItem`，用点击处理把 `DataContext as TrayCell` 写入 `vm.SelectedTrayCell`（非 code-behind.Children.Add，符合架构红线）。
- **构建坑**：`Border` 不能既写 `Style="{StaticResource ...}"` 属性又写内层 `<Border.Style>`——去掉外层属性，内层 `BasedOn` 即可。另：dotnet 是 Windows 程序，`/d/temp/...` 会被当成开关，必须传 `D:/temp/...` Windows 路径。
- **验证**：`tar` → `D:\temp\ncv_hw5b` → 系统 .NET 10 SDK 后台构建：**0 错误 0 警告**；输出含 `NoCodeVision.exe` + `GrayModelNative.dll` + `opencv_world480.dll` + `OpenCvSharpExtern.dll`；`timeout 8 ./NoCodeVision.exe` 存活无崩溃。

## ncv_hw6：轴点位表/料盘支持多个（左侧列表增删重命名）

- **需求**：轴点位表要多个、料盘要多个，左侧列表可新增/删除/重命名。即这两个 tab 升级为「集合列表 + 集合内条目」两级。
- **VM（`VisionMotionViewModels.cs`，本伦 Edit 工具可直接改，加密干扰已消失）**：
  - 新增 `PointTableGroup`（Name + `ObservableCollection<PointRow> Points`）、`TrayGroup`（Name + Rows/Cols + `GridText` + `ObservableCollection<TrayCell> Cells`）。
  - 原 `PointTable`/`TrayCells` 改为 `PointTables`/`Trays`（`ObservableCollection<PointTableGroup>`/`TrayGroup`）；新增选择属性 `SelectedPointTable`/`SelectedTray`，构造里预置「默认点位表」「默认料盘」并默认选中。
  - `AddCmd/DeleteCmd/RenameCmd` 的 `switch(SelectedTab)` 中「轴点位表」「料盘」改为对集合（group）增删重命名；`DeleteCmd` 的 canExecute 改为 `switch` 表达式按 tab 校验当前选中项。
  - 新增 `AddPointCmd/DeletePointCmd/RenamePointCmd`，仅对「轴点位表」选中表内的点位生效（`canExecute` 校验 `SelectedPointTable`/`SelectedPoint`）。
  - 料盘新增走 `TrayGroup` 并按 Rows×Cols 预填格子；删除/重命名作用于选中料盘。原 `TrayRows`/`TrayCols` 属性保留但未再用。
- **XAML（`Views/MotionControlView.xaml`）**：
  - 轴点位表：左列表 `PointTables`（列：点位表名/点数），右栏嵌套 Grid = 左「点位列表」（`SelectedPointTable.Points`，含 新增点/删除点/重命名点 + 点位名/X/Y/Z）+ 右「点位参数」属性面板。
  - 料盘：左列表 `Trays`（列：料盘名/行×列，补回「新增」按钮），右栏嵌套 Grid = 左「格子网格」（`SelectedTray.Cells`，UniformGrid 绑 `SelectedTray.Rows/Cols`，点击 `TrayCell_Click` 写入 `SelectedTrayCell`）+ 右「料盘格参数」属性面板。
  - 轴/IO/气缸 tab 结构不变。
- **验证**：`tar` → `D:\temp\ncv_hw6` → 系统 .NET 10 SDK 后台构建：**0 错误 0 警告**；四产物齐全；`timeout 8 ./NoCodeVision.exe` 存活无崩溃。

## ncv_hw8：流程页增加运控工具 + 全属性面板参数扩充

- **需求**（截图 + 文字）：流程工具箱要加「运控」类工具；且「每个工具的右侧属性都要尽量多的参数」。
- **VM（`ViewModels.cs`，本伦 Edit 工具可直接改，加密干扰已消失）**：
  - `StepType` 仍是字符串（无枚举）。`StepFunctions` 由 5 项扩为 **10 项**：图像采集/模板匹配/几何测量/逻辑判断/结果输出/**轴运动/IO控制/气缸动作/等待延时/通讯指令**。
  - `VisionFlowStep` 新增运动参数：轴运动(`TargetAxis/MoveType/MoveMode/TargetX/Y/Z/Speed/Accel/Decel/InPosTimeout/ServoOn`)、IO(`IoChannel/IoType/IoValue/WaitDone`)、气缸(`CylName/CylAction/CylTimeout/SensorIn/SensorOut`)、等待(`WaitType/WaitTime/WaitCondition`)、通讯(`CommChannel/CommCmd/CommContent/CommEncoding`)。
  - 扩充既有面板参数：图像采集(`Exposure/Gain/TriggerMode/PixelFormat/ImageWidth/Height/AutoExposure/SaveImage/ImageFormat`)、模板匹配(`SortBy/BorderMode/UseMask/MinDistance`)、几何测量(`MeasureUnit/Decimals/CaliperCount/EdgePolarity/EdgeThreshold/OutputVar`)、逻辑(`CompareVar/CompareSource/Remark`)、输出(`OutputType/DataType/Trigger/Verify/OnFail`)。
  - `FlowViewModel` 新增对应 `ItemsSource` 数组：``；`StatusOptions` 增补 运动完成/到位/未到位/动作完成。
  - `AddStepCmd` 的 `switch` 映射 5 个新函数 → StepType(`AxisMove/IOControl/Cylinder/Wait/Comm`) 与图标(🦾🔌🟢⏱📡)；新步默认进 `RunStep` 的 `default` 分支（ActualValue="OK"，无硬件接线，不报错）。
- **XAML（`Views/FlowView.xaml`）**：
  - 图像采集：曝光/增益改绑 `Exposure/Gain`，新增 触发模式/像素格式/分辨率宽高/图像格式/自动曝光/保存原图(勾选)。
  - 模板匹配：相似度阈值后新增 结果排序/边缘填充/最小匹配间距/启用掩膜(勾选)。
  - 几何测量：公差后新增 单位/小数位/卡尺数/边缘极性/边缘阈值/输出变量。
  - 逻辑判断：False 分支后新增 比较变量/比较来源/备注。
  - 结果输出：输出值后新增 输出类型/数据类型/触发方式/失败处理/写后读校验(勾选)。
  - 新增 5 个 `Border` 属性面板，各自 `DataTrigger` 绑 `SelectedStep.StepType` 显隐：轴运动(轴/类型/模式/XYZ 目标/速度·加速度·减速度滑块/到位超时/伺服使能)、IO控制(类型/通道/输出值/等待完成)、气缸动作(名称/动作/到位·缩回传感器/超时)、等待延时(类型/时间/条件)、通讯指令(通道/类型/编码/内容)。复用了 `IntToBoolConverter`（文件内已声明）。
- **验证**：`tar` → `D:\temp\ncv_hw8` → 系统 .NET 10 SDK 后台构建：**0 错误 0 警告**；四产物齐全(`NoCodeVision.exe`+`GrayModelNative.dll`+`opencv_world480.dll`+`OpenCvSharpExtern.dll`)；`timeout 8 ./NoCodeVision.exe` 存活无崩溃(exit 124)。
