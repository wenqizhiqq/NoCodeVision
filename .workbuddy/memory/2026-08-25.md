# 2026-08-25

- ncv_scriptflow_lua_editor_enhance（ScriptFlowView）：
  - Lua 语法高亮：`Views/ScriptFlowView.Partial.cs` 内 `ApplyLuaHighlighting()` 用内嵌 XSHD 字符串（`LuaXshd` 常量）经 `HighlightingLoader.Load` 加载，绑定到 `Editor.SyntaxHighlighting`。配色：变量(identifier)=青绿 #0F766E，逻辑关键字(if/then/for/function/local/...)=紫 #9333EA，字符串=绿 #16A34A，数字=橙 #C2410C，注释(--)=灰 #8E8E93。Light 主题可读。
  - 点击变量取名称+值：`Editor.TextArea.MouseUp` 与 `Caret.PositionChanged` 触发 `UpdateVariableAtCaret()`，取光标词，在 `FlowViewModel.Variables`（`NoCodeVision.Scripting.VarItem` 集合，运行/断点后填充）查找同名，写入 `SelectedVariable`；未找到则构造临时 `VarItem` 显示「未监视/未运行」。右侧「变量检查」面板绑定 `SelectedVariable` 即显示。
  - 左侧耗时显示：`ScriptFlowView.xaml` 编辑器标题条左侧加「⏱ 耗时 {ScriptElapsedText}」（ms/s 由 VM `ScriptElapsedText` 提供）。
  - 注意：`VarItem` 命名空间歧义——`NoCodeVision.Scripting.VarItem`(Lua运行时) 与 `NoCodeVision.ViewModels.VarItem`(变量页面) 同名，Partial.cs 需全名引用 Scripting 版。
  - 构建验证：`dotnet build -c Debug` 0 错误。
- ncv_measure_layout_tips（FlowView 几何测量）：
  - 右侧标题在 StepType=Measure 时改为「测量类型 - 步骤名」（例如「圆径 - 几何测量」），避免「几何测量 - 几何测量」重复。
  - 顶部新增「测量摘要」卡片：明确显示测量目标（类型 + 标称值 ± 公差 + 单位）、输出变量、判定逻辑；右侧预留「实测值 / 判定」占位，提示运行后显示结果。
  - 参数改用两列分组布局：「测量规格」（类型/标称/公差/单位/小数位/输出变量）与「算法参数」（卡尺数/边缘极性/边缘阈值/ROI 坐标），每个参数都加 ToolTip。
  - ROI 区域加大到 240、提示更详细：说明运行后显示结果，以及工具用法。
  - 原计划扩展 `VisionFlowStep` 加 `MeasuredValue`/`MeasureResultText`，但 `ViewModels.cs` 加密且类未标记 `partial`，无法 partial 扩展；改为在摘要中预留运行后结果显示占位，后续可在 `FlowViewModel` 或业务层补充实测值写入逻辑。
  - 构建验证：`dotnet build -c Debug` 0 错误。

- ncv_patent_timing_layout_compress（时序编排卡片压缩成2行）：
  - 需求（截图）：FlowView 右侧「时序确定性编排」卡片当前 时序标记/同步组/总线周期/偏差阈值 占3行内容，要求压缩。
  - 改动：`Views/FlowView.xaml` 中编排卡片的内容区改为 2 行 × 2 列 Grid：
    - 第1行：选中步骤·时序标记 (T+0ms / T+5ms) | 选中步骤·同步组 (如 GroupA)
    - 第2行：总线周期(ms) | 偏差报警阈值(ms)
  - 移除原 总线周期/偏差阈值 单独占行的 TextBlock+TextBox；4 个输入框统一填满各自列，不再固定 120px 宽度；标签下边距从 4 降到 2，行间距 8，整体更紧凑。
  - 构建验证：`dotnet build -c Debug` 0 错误，仅 LuaDebugHost.cs 既有可空警告。

- ncv_patent_timing_move_bottom（时序编排卡片移到右侧面板底部）：
  - 需求（截图）：FlowView 右侧属性面板把「时序确定性编排」卡片从顶部移到最底部。
  - 改动：`Views/FlowView.xaml` 用 Python 把该卡片（comment+Border）从右侧 ScrollViewer 内 StackPanel 首位（原 line 239）剪切，移动到容器 `</StackPanel>` 之前（最后一张步骤卡 Comm 之后），保持 16 空格缩进与其它卡片一致；顶部改为直接显示步骤标题 TextBlock。
  - 构建验证：`dotnet build -c Debug` 0 错误。

## ncv_manual_page（顶部导航末位「说明书」页）
- 时间: 10:44 用户需求「写一个说明书页面到最后」
- 需求: 在顶部分段导航的**最后**新增一个「说明书」页（手册/帮助），用 Apple/iOS 风格呈现软件各模块用法。
- 改动文件:
  - `Themes/Icons.xaml`: 新增 `IcoManual`（蓝色书本 DrawingImage，#2563EB 封面 + #1D4ED8 书脊 + #DBEAFE 文字行）。注意此前曾误插入一个孤立 `<` 行导致 MC3000 解析错，已删除；并统一为 LF 行尾。
  - `Views/ManualView.xaml`(新增): UserControl + ScrollViewer，含 5 张 `AppleGroupCard`：① 顶部导航与功能模块（项目/相机/运控/通讯/变量/流程/工程师/操作员 各一行）② 快速上手 5 步（AppleStepIndex 药丸序号）③ 表格化时序确定性编排（时序标记/同步组/⏱时序编译/▶时序运行）④ 运行面板按钮颜色约定（Run绿#34C759/Pause橙#FF9500/Stop红#FF3B30/E-Stop红#FF3B30/Reset蓝#007AFF，真实驱动硬件）⑤ 通讯能力（Modbus/Socket/串口/OPC UA/PLC）。顶部 `iOSTitle`「软件说明书」+ `AppleHintBubble` 简介。
  - `Views/ManualView.xaml.cs`(新增): 极简 partial，无 VM（静态内容）。
  - `MainWindow.xaml`: 在 Nav7(操作员) 之后新增 `Nav8` 分段按钮（Tag=8，IcoManual，文字「说明书」）。
  - `MainWindow.xaml.cs`: `_views` 数组末尾 `new ManualView()`，`_navButtons` 末尾追加 `Nav8`（共 9 个）。
- 构建: `dotnet build -c Debug` → 0 错误，仅 LuaDebugHost.cs 既有 10 个可空警告。
- 验证: VS Rebuild + F5，顶部导航最右侧出现「说明书」页签，点击进入查看手册。
- 坑: Icons.xaml 原文件为 CRLF，插入时产生孤立 `<` 行 + 行尾混乱导致 MC3000；最终改为纯 LF 并删除孤立行解决。XAML 资源字典行尾务必保持与同类文件一致，且不要在 `</ResourceDictionary>` 前残留脏行。

- ncv_variables_page_beautify（Views/VariablesView.xaml 布局美化）：
  - 补齐列：DataGrid 原仅显示 变量名称/变量值，新增「类型」(Type, 固定100宽, 次级灰字) 与「备注」(Remark, 静默灰字) 两列，VM `NoCodeVision.ViewModels.VarItem` 已有这四个属性，信息更完整、布局更均衡。
  - 工具栏统一：新增 `FieldBox` 资源（圆角9 / LineBrush描边 / SurfaceBrush底 / Padding 10,5），把搜索框与 Excel 路径框都包进同一张 `AppleGroupCard` 操作卡（Padding=14,12），中间加 1px LineBrush 分隔线，消除原来 Excel 栏与搜索栏视觉割裂。
  - 细节：Excel 行改为 DockPanel（左：Excel 路径 label+FieldBox 路径框 宽240；右：导出/导入按钮 + ExcelStatus），路径框 ToolTip 注明「自动避开系统文件对话框拦截」；按钮 Padding 统一 16,7 / 14,7；搜索框 ToolTip 扩展为「按名称、类型或值搜索」。
  - 构建验证：`dotnet build -c Debug` 0 错误，仅 LuaDebugHost.cs 既有可空警告。
