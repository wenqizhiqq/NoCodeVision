# 2026-09-14 工作记录

## 新建项目示例弹窗 + 缺陷检测后外发外部软件

需求：新建项目时弹窗选择示例（带缩略图），示例区分「硬件触发/软件触发 × 相机采集/读取文件图片」组合，并在缺陷检测完成后把结果发送给外部软件。

### 改动（全部明文层，未碰 45 个 88 7d 1c 加密文件）
- 生成 5 张示例缩略图 `Resources/Examples/*.png`（PIL + msyh.ttc），并在 `NoCodeVision.csproj` 注册为 `<Resource>`（pack URI 引用）。
- `ViewModels/ViewModels.cs`：
  - `ProjectItem` 新增字段：`ImageSourceType`(采集相机/打开文件/打开文件夹)、`TriggerType`(硬件触发/软件触发)、`SendResultToExternal`、`ExternalChannel`(MQTT/网口/UDP/串口)、`ExternalTarget`、`ExampleId`。
  - 新增 `ProjectExample` 类（4 个示例：硬件·相机/软件·文件/硬件·文件/软件·相机，含 `Thumbnail` pack URI）。
  - `ProjectViewModel` 新增 `Examples`/`Instance`/`IsExampleDialogOpen`/`SelectedExample` 及 `ShowExamplesCmd`/`ConfirmExampleCmd`(按参数取示例→ToProjectItem→落盘)/`CancelExampleCmd`/`CreateBlankCmd`。
  - `FlowViewModel.case "Defect"` 检测完成后调用 `TrySendDefectResultToExternal(defects)`：若 `SelectedProject.SendResultToExternal`，按 `ExternalChannel`/`ExternalTarget` 构造 `CommConfigItem`、必要时 `CommHub.Instance.ConnectAsync` 后 `SendAsync(JSON 结果)`（字典序列化，全程 try/catch）。
- `Views/ProjectView.xaml`：
  - 「新建」按钮改绑 `ShowExamplesCmd`；新增 `BooleanToVisibilityConverter` 资源。
  - 新增「采集与触发/外发」配置卡片（采集源/触发方式/检测后外发/外发通道/外发地址）。
  - 新增模态弹窗覆盖层：示例卡片（缩略图+名称+触发/采集标签+外发徽标），点卡片即 `ConfirmExampleCmd`；底部「空白项目」「取消」。

### 构建
`dotnet build NoCodeVision.csproj -c Debug` → **0 错误**（仅既有可空警告，本次无关）。

### 注意
- `CommHub`（`NoCodeVision.Comm`，加密）在 plaintext 可透明调用；`CommConfigItem` 同命名空间。
- 外发依赖「通讯」页已连接通道；未连接时本逻辑会尝试自动 ConnectAsync 再 SendAsync，失败仅写 Status 不抛错。
- bin 输出在 VS Rebuild 会被清空；示例缩略图已注册为嵌入资源（Resource），重建自动重新嵌入，不依赖 bin 残留。
- 工具链：文件写入一律走 Python `io.open`（Write 工具写 .py 会被 IDE 写钩加密成 88 7d 1c 损坏）；含中文脚本存 .txt 经 PowerShell 调 python 运行。

## 修正（08:34 截图反馈）
用户反馈示例弹窗应由左侧「项目列表」的「+ 添加」按钮触发（而非详情页「新建」按钮）。已修正 `Views/ProjectView.xaml`：
- `AppleMasterList AddCommand="{Binding ShowExamplesCmd}"`（原 `AddCmd`）。
- 详情页操作区「新建」按钮还原为 `Command="{Binding NewCmd}"`。

## 弹窗修正（08:38 截图反馈：选择不了 / 弹窗错误）
问题根因：
1. 弹窗覆盖层未设置 `Grid.ColumnSpan="2"`，默认只占据左列，导致弹窗被挤到左侧且被左列宽度裁剪。
2. 示例卡片命令绑定写法错误：`{Binding ConfirmExampleCmd, RelativeSource={RelativeSource AncestorType=UserControl}}` 指向 UserControl 自身属性；改为 `DataContext.ConfirmExampleCmd` 才真正命中 ViewModel 命令，所以之前点卡片不响应。

已重写 `Views/ProjectView.xaml` 弹窗覆盖层：
- `Grid.ColumnSpan="2"` + `Panel.ZIndex="100"`：全视图覆盖，确保在最上层。
- 命令修正为 `Command="{Binding DataContext.ConfirmExampleCmd, RelativeSource={RelativeSource AncestorType=UserControl}}"`。
- 示例卡片放大（220×240）、底部按钮间距加大、白色不透明对话框底。
构建 0 错误。
